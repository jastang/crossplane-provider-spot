---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: aksnpvirtualnodegroups.aksnpvng.spot.upbound.io
spec:
  group: aksnpvng.spot.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - spot
    kind: AksNpVirtualNodeGroup
    listKind: AksNpVirtualNodeGroupList
    plural: aksnpvirtualnodegroups
    singular: aksnpvirtualnodegroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AksNpVirtualNodeGroup is the Schema for the AksNpVirtualNodeGroups
          API. Provides a Spotinst Ocean Virtual Node Group resource using AKS.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AksNpVirtualNodeGroupSpec defines the desired state of AksNpVirtualNodeGroup
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicy field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  availabilityZones:
                    description: An Array holding Availability Zones, this configures
                      the availability zones the Ocean may launch instances in per
                      VNG.
                    items:
                      type: string
                    type: array
                  enableNodePublicIp:
                    description: Enable node public IP.
                    type: boolean
                  fallbackToOndemand:
                    description: If no spot instance markets are available, enable
                      Ocean to launch on-demand instances instead.
                    type: boolean
                  filters:
                    description: Filters for the VM sizes that can be launched from
                      the virtual node group.
                    items:
                      properties:
                        architectures:
                          description: The filtered vm sizes will support at least
                            one of the architectures from this list. x86_64 includes
                            both intel64 and amd64.
                          items:
                            type: string
                          type: array
                        maxMemoryGib:
                          description: Maximum amount of Memory (GiB).
                          type: number
                        maxVcpu:
                          description: Maximum number of vcpus available.
                          type: number
                        minMemoryGib:
                          description: Minimum amount of Memory (GiB).
                          type: number
                        minVcpu:
                          description: Minimum number of vcpus available.
                          type: number
                        series:
                          description: Vm sizes belonging to a series from the list
                            will be available for scaling.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  headrooms:
                    description: Specify the custom headroom per VNG. Provide a list
                      of headroom objects.
                    items:
                      properties:
                        cpuPerUnit:
                          description: Configure the number of CPUs to allocate the
                            headroom. CPUs are denoted in millicores, where 1000 millicores
                            = 1 vCPU.
                          type: number
                        gpuPerUnit:
                          description: Amount of GPU to allocate for headroom unit.
                          type: number
                        memoryPerUnit:
                          description: Configure the amount of memory (MiB) to allocate
                            the headroom.
                          type: number
                        numOfUnits:
                          description: The number of units to retain as headroom,
                            where each unit has the defined headroom CPU and memory.
                          type: number
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: An array of labels to add to the virtual node group.Only
                      custom user labels are allowed, and not Kubernetes built-in
                      labels or Spot internal labels.
                    type: object
                  maxCount:
                    description: Maximum node count limit.
                    type: number
                  maxPodsPerNode:
                    description: The maximum number of pods per node in the node pools.
                    type: number
                  minCount:
                    description: Minimum node count limit.
                    type: number
                  name:
                    description: Enter a name for the virtual node group.
                    type: string
                  oceanId:
                    description: The Ocean cluster identifier. Required for Launch
                      Spec creation.
                    type: string
                  osDiskSizeGb:
                    description: The size of the OS disk in GB.
                    type: number
                  osDiskType:
                    description: The type of the OS disk.
                    type: string
                  osType:
                    description: The OS type of the OS disk.
                    type: string
                  spotPercentage:
                    description: Percentage of spot VMs to maintain.
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                  taints:
                    description: Add taints to a virtual node group.
                    items:
                      properties:
                        effect:
                          description: Set taint effect.
                          type: string
                        key:
                          description: 'Set label key. The following are not allowed:
                            ["kubernetes.azure.com/agentpool", "kubernetes.io/arch",
                            "kubernetes.io/os", "node.kubernetes.io/instance-type",
                            "topology.kubernetes.io/region", "topology.kubernetes.io/zone",
                            "kubernetes.azure.com/cluster", "kubernetes.azure.com/mode",
                            "kubernetes.azure.com/role", "kubernetes.azure.com/scalesetpriority",
                            "kubernetes.io/hostname", "kubernetes.azure.com/storageprofile",
                            "kubernetes.azure.com/storagetier", "kubernetes.azure.com/instance-sku",
                            "kubernetes.azure.com/node-image-version", "kubernetes.azure.com/subnet",
                            "kubernetes.azure.com/vnet", "kubernetes.azure.com/ppg",
                            "kubernetes.azure.com/encrypted-set", "kubernetes.azure.com/accelerator",
                            "kubernetes.azure.com/fips_enabled", "kubernetes.azure.com/os-sku"]'
                          type: string
                        value:
                          description: Set label value.
                          type: string
                      required:
                      - effect
                      - key
                      - value
                      type: object
                    type: array
                type: object
              managementPolicy:
                default: FullControl
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicy
                  specifies the level of control Crossplane has over the managed external
                  resource. This field is planned to replace the DeletionPolicy field
                  in a future release. Currently, both could be set independently
                  and non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - FullControl
                - ObserveOnly
                - OrphanOnDelete
                type: string
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: name is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.name)
            - message: oceanId is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.oceanId)
          status:
            description: AksNpVirtualNodeGroupStatus defines the observed state of
              AksNpVirtualNodeGroup.
            properties:
              atProvider:
                properties:
                  availabilityZones:
                    description: An Array holding Availability Zones, this configures
                      the availability zones the Ocean may launch instances in per
                      VNG.
                    items:
                      type: string
                    type: array
                  enableNodePublicIp:
                    description: Enable node public IP.
                    type: boolean
                  fallbackToOndemand:
                    description: If no spot instance markets are available, enable
                      Ocean to launch on-demand instances instead.
                    type: boolean
                  filters:
                    description: Filters for the VM sizes that can be launched from
                      the virtual node group.
                    items:
                      properties:
                        architectures:
                          description: The filtered vm sizes will support at least
                            one of the architectures from this list. x86_64 includes
                            both intel64 and amd64.
                          items:
                            type: string
                          type: array
                        maxMemoryGib:
                          description: Maximum amount of Memory (GiB).
                          type: number
                        maxVcpu:
                          description: Maximum number of vcpus available.
                          type: number
                        minMemoryGib:
                          description: Minimum amount of Memory (GiB).
                          type: number
                        minVcpu:
                          description: Minimum number of vcpus available.
                          type: number
                        series:
                          description: Vm sizes belonging to a series from the list
                            will be available for scaling.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  headrooms:
                    description: Specify the custom headroom per VNG. Provide a list
                      of headroom objects.
                    items:
                      properties:
                        cpuPerUnit:
                          description: Configure the number of CPUs to allocate the
                            headroom. CPUs are denoted in millicores, where 1000 millicores
                            = 1 vCPU.
                          type: number
                        gpuPerUnit:
                          description: Amount of GPU to allocate for headroom unit.
                          type: number
                        memoryPerUnit:
                          description: Configure the amount of memory (MiB) to allocate
                            the headroom.
                          type: number
                        numOfUnits:
                          description: The number of units to retain as headroom,
                            where each unit has the defined headroom CPU and memory.
                          type: number
                      type: object
                    type: array
                  id:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: An array of labels to add to the virtual node group.Only
                      custom user labels are allowed, and not Kubernetes built-in
                      labels or Spot internal labels.
                    type: object
                  maxCount:
                    description: Maximum node count limit.
                    type: number
                  maxPodsPerNode:
                    description: The maximum number of pods per node in the node pools.
                    type: number
                  minCount:
                    description: Minimum node count limit.
                    type: number
                  name:
                    description: Enter a name for the virtual node group.
                    type: string
                  oceanId:
                    description: The Ocean cluster identifier. Required for Launch
                      Spec creation.
                    type: string
                  osDiskSizeGb:
                    description: The size of the OS disk in GB.
                    type: number
                  osDiskType:
                    description: The type of the OS disk.
                    type: string
                  osType:
                    description: The OS type of the OS disk.
                    type: string
                  spotPercentage:
                    description: Percentage of spot VMs to maintain.
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                  taints:
                    description: Add taints to a virtual node group.
                    items:
                      properties:
                        effect:
                          description: Set taint effect.
                          type: string
                        key:
                          description: 'Set label key. The following are not allowed:
                            ["kubernetes.azure.com/agentpool", "kubernetes.io/arch",
                            "kubernetes.io/os", "node.kubernetes.io/instance-type",
                            "topology.kubernetes.io/region", "topology.kubernetes.io/zone",
                            "kubernetes.azure.com/cluster", "kubernetes.azure.com/mode",
                            "kubernetes.azure.com/role", "kubernetes.azure.com/scalesetpriority",
                            "kubernetes.io/hostname", "kubernetes.azure.com/storageprofile",
                            "kubernetes.azure.com/storagetier", "kubernetes.azure.com/instance-sku",
                            "kubernetes.azure.com/node-image-version", "kubernetes.azure.com/subnet",
                            "kubernetes.azure.com/vnet", "kubernetes.azure.com/ppg",
                            "kubernetes.azure.com/encrypted-set", "kubernetes.azure.com/accelerator",
                            "kubernetes.azure.com/fips_enabled", "kubernetes.azure.com/os-sku"]'
                          type: string
                        value:
                          description: Set label value.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
